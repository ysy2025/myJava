访问者模式:
    封装一些作用于某种数据结构的各元素的操作,可以再不改变数据结构的前提下,定义作用于这些元素的新操作.
    主要是将数据结构和数据操作分离,解决数据结构和操作耦合性问题
    在被访问的类中增加一个对外提供接待访问者的接口

    应用场景:需要对一个对象结构中的对象,进行很多不同操作,彼此没有关联,同事需要避免让这些操作污染类.可以选用访问者模式解决.

    双分派:不管分类如何变化,都能找到期望的方法运行
    双分派意味着得到执行的操作,取决于请求的种类和两个接受者的类型

    假如需要添加一个wait类,考察man和woman
